# Publish to GitHub

```bash
cd causal-llm-world-models
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin <YOUR_GITHUB_REPO_URL>
git push -u origin main
```

If the raw results become too large over time, consider Git LFS or GitHub Releases.
